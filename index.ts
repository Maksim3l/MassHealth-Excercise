// index.tsx
import { registerRootComponent } from 'expo';

// Import your layout
import Layout from './app/_layout';

// Register the root component (which will be the layout file)
registerRootComponent(Layout);
